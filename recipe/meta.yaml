{% set name = "fast_matrix_market" %}
{% set version = "1.5.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/fast-matrix-market-{{ version }}.tar.gz
  sha256: 99b35d346d18c98ebbb92118ce3bf3a0276ab961fc00f53dd271c2915e94ab1c

build:
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  build:
    - {{ compiler('cxx') }}
    - cmake
  host:
    - python
    - scikit-build-core
    - pybind11
    - pip
  run:
    - python
    - libstdcxx-ng  # [linux]
    - libcxx  # [osx]

test:
  imports:
    - fast_matrix_market
  commands:
    - pip check
    - pytest tests
  requires:
    - pip
    - pytest
    - scipy
  source_files:
    - tests/


about:
  home: https://github.com/alugowski/fast_matrix_market
  summary: Fast and full-featured Matrix Market file I/O
  license: BSD-2-Clause
  license_file:
    - LICENSE.txt

extra:
  recipe-maintainers:
    - alugowski
